Задачи:
- найти в апи краткую инфу или в зависимости от того какой раздел нужно изменять состав карточек (возможно понад доп компонент)

Вопросы:
- откуда везде рендерится точка с запятой
- почему не добавляются ключи к элементам списка

Заметки:
- при добавлении эл-та в избр перерендеривается список, но список в сторе почему-то пуст
- не меняется текущий раздел в сторе
- когда рендерю нед просм, после возврата всех li, запускается экшен и редьюсер с данными из dataApi



* - важно
~ - под вопросом
- - заметка об ошибке

Nav:
~ меняется адрес у ссылки на главную (восстановилось)

SearchBar:
- поисковик на название не работает
- неправильно работает + при фильтрации по дате рендерятся li без текста
- неправильный текущий раздел
- передавать в него название раздела для корректирования радиокнопок

Pagination:
- номер страницы не меняется
- при клике не рендерится список (возможно нужна мидлвара)
- посмотреть, как делать запросы постранично

ListItem:
* свич не изменят контент, из-за этого рендерится пустой список
- не устанавливаются ключи
- сделать стили для кнопки и флексом выровнять по правому краю
- передавать в него название раздела для корректирования списка краткой инфы
- при изменении содержимого в свиче рендерится одно и то же
- клик на svg перестал работать когда остановила всплытие на кнопке

DetailedInformation:
- пытается отрендериться List вот только данные приходят в виде объекта, а не массива

Favorites:
- когда удаляется элемент нужно перерендерить список
- условие на пропуск первого свойства (счетчик)
- в редьюсер приходит пустой стейт

RecentlyViewed:
- не заходит в List, запускает перерендер
